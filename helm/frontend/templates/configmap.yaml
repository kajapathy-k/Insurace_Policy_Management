apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  namespace: {{ include "frontend.namespace" . }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  REACT_APP_API_URL: {{ .Values.configMap.data.REACT_APP_API_URL | quote }}
