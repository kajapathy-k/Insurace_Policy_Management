apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: insurance-gateway
  namespace: insurance
  labels:
    app.kubernetes.io/name: insurance-gateway
    app.kubernetes.io/part-of: insurance-platform
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
